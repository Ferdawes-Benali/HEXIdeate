# ── Core vision ───────────────────────────────────────────────────────────────
opencv-python-headless>=4.9.0
mediapipe>=0.10.9
ultralytics>=8.2.0               # YOLOv8 (local ONNX fallback + COCO sirop)

# ── Roboflow inference API ────────────────────────────────────────────────────
inference-sdk>=0.9.0             # pip install inference-sdk
# Set env vars in .env (see robotflowdetector.py):
#   ROBOFLOW_API_KEY     = your key
#   ROBOFLOW_WORKSPACE   = meriam-cherif
#   ROBOFLOW_PROJECT     = pills-detection-s9ywn
#   ROBOFLOW_VERSION     = 19
#   ROBOFLOW_WORKFLOW_ID = your-workflow-slug  (optional — leave empty for model endpoint)
#   USE_WORKFLOW         = false

# ── OCR ───────────────────────────────────────────────────────────────────────
pytesseract>=0.3.10
# Tesseract binary:
#   Ubuntu/Debian : sudo apt-get install tesseract-ocr tesseract-ocr-fra tesseract-ocr-ara
#   macOS         : brew install tesseract
#   Windows       : https://github.com/UB-Mannheim/tesseract/wiki

# ── Fuzzy matching ────────────────────────────────────────────────────────────
rapidfuzz>=3.6.1

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy>=1.26.0
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.6.0